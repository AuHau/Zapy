   def ZaUser_delete(self):
      """
         Zapi Call to $ZaUser.delete
      """

      info = None

      cmd = {
         "cmd":"$ZaUser.delete",
         "args": {
	    "account": self._cfg['account']
	 }
      }

      info = self.makeZapyRequest(json.dumps(cmd))

      return info
